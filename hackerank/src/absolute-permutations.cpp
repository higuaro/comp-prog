#include <map>
#include <set>
#include <list>
#include <cmath>
#include <ctime>
#include <deque>
#include <queue>
#include <stack>
#include <string>
#include <bitset>
#include <cstdio>
#include <limits>
#include <vector>
#include <climits>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <numeric>
#include <sstream>
#include <iostream>
#include <algorithm>
#include <unordered_map>

using namespace std;

// std::string solve(const int n, const int k) {
//     std::vector<int> numbers(n);
//     std::itoa(std::begin(numbers), std::end(numbers), 1);

// }

int main(){
    // int t;
    // cin >> t;
    // for(int a0 = 0; a0 < t; a0++){
    //     int n;
    //     int k;
    //     cin >> n >> k;
    // }
    std::cout << (1 << -15) << std::endl;
    return 0;
}
