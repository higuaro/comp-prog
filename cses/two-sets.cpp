#include <bits/stdc++.h>
using namespace std;
vector<uint64_t> solve(vector<uint64_t>& a, const uint64_t t, uint64_t k, int i) {
  if (k <= 0) return ;
  
}
int main() {
  uint64_t n; cin >> n;
  if (!(n & 1)) {
    cout << "NO" << endl;
    return 0;
  }
  uint64_t t = n * (n + 1) / 4;
  set<uint64_t> a;

  for (int k = 1; k < n; k++) 
    
  return 0;
}
